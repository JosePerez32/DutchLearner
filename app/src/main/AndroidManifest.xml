<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.perez.dutchlearner">

    <!-- Permisos necesarios<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" /> -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Para WorkManager en Android 12+ -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Agregar después de las actividades -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="com.android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-feature android:name="android.hardware.alarm" android:required="false" />
    <uses-feature
        android:name="android.hardware.alarm"
        android:required="false" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DutchLearner"
        tools:targetApi="31">
        <!-- Foreground Service para grabación en background -->
        <service
            android:name=".widget.RecordingForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />
        <service
            android:name=".widget.RecordingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.DutchLearner">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Inicializar WorkManager -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- Dentro de <application> -->
        <receiver
            android:name=".notifications.AlarmReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.perez.dutchlearner.NOTIFICATION_ALARM" />
            </intent-filter>
        </receiver>
        <!-- Widget de grabación rápida -->

        <!-- Widget de grabación rápida (NUEVO con Service) -->
        <receiver
            android:name=".widget.QuickRecordWidget"
            android:exported="true"
            android:label="Dutch Learner - Quick Record">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.perez.dutchlearner.widget.ACTION_RECORD" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quick_record_widget_info" />
        </receiver>

        <!-- Widget anterior (abre app) - OPCIONAL: puedes eliminarlo si solo quieres el nuevo -->
        <receiver
            android:name=".widget.RecordWidget"
            android:exported="true"
            android:label="Dutch Learner - Open App">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_record_info" />
        </receiver>
    </application>

</manifest>